#!/bin/bash
#PBS -N AutoP_v1_Yields
#PBS -q cpu
#PBS -l select=1:ncpus=32:mem=32gb
#PBS -l walltime=10:00:00
#PBS -o logs/
#PBS -e logs/

cd $PBS_O_WORKDIR

module purge
module load compiler/anaconda3-23.07.0

source venv/bin/activate

# ── Config ───────────────────────────────────────────────────────────────────
SOURCE="mp"             # rs (revscope) | mp (minpaths)
VERSION="1"             # autonet version
MODE="sbd"              # sbd (split-by-demand) | iter (iterative)
NUM_WORKERS=32          # parallel workers

# MinPaths (mp) settings — only used when SOURCE=mp
PRUNER="batch"          # batch | single
PRUNING="prune"         # prune | noprune
PATHS_VERSION="2"       # minpath dataset version
# ─────────────────────────────────────────────────────────────────────────────

if [ "$SOURCE" = "mp" ]; then
    python get_autoNet_yields.py $SOURCE $VERSION $MODE $NUM_WORKERS $PRUNER $PRUNING $PATHS_VERSION
else
    python get_autoNet_yields.py $SOURCE $VERSION $MODE $NUM_WORKERS
fi

echo "Finished yield calculation at $(date)"
